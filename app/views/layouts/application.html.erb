<!DOCTYPE html>
<html>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <head>
    <title><%= content_for(:title) || "Docketwise Pro" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Docketwise Pro">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm mb-4">
      <div class="container">
        <%= link_to "⚖️ Docketwise Pro", root_path, class: "navbar-brand fw-bold" %>
        
        <div class="d-flex align-items-center">
          <% if user_signed_in? %>
            <span class="text-light me-3">
              👤 <%= current_user.email %>
            </span>
            <%= button_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger btn-sm fw-bold" %>
          <% else %>
            <%= link_to "Iniciar Sesión", new_user_session_path, class: "btn btn-primary btn-sm fw-bold me-2" %>
            <%= link_to "Registrarse", new_user_registration_path, class: "btn btn-outline-light btn-sm fw-bold" %>
          <% end %>
        </div>
      </div>
    </nav>
    
    <main class="container mt-5">
      <%= yield %>
    </main>

    <footer class="text-center mt-5 mb-4 text-muted border-top pt-3">
      <small>
        <strong>Docketwise Pro Clon</strong> &copy; <%= Time.current.year %> | 
        Desarrollado por Hugo 🚀 (Ingeniero Aumentado)
      </small>
    </footer>
  </body>
</html>
